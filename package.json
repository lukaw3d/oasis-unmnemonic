{
  "scripts": {
    "start": "parcel serve --no-hmr index.html",
    "build": "rm -rf docs && parcel build --no-optimize --no-scope-hoist --no-source-maps --public-url /unmnemonic --dist-dir docs index.html",
    "test": "node test.js"
  },
  "dependencies": {
    "@oasisprotocol/client": "^0.1.1-alpha.2",
    "@zondax/ledger-substrate": "^0.24.0"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "parcel": "^2.3.2",
    "process": "^0.11.10",
    "stream-browserify": "^3.0.0"
  }
}
